<UserControl x:Class="AuroraMusic.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Diagnostics" FontSize="18" FontWeight="SemiBold"/>
            <Button Content="Refresh" Margin="12,0,0,0" Command="{Binding RefreshCommand}"/>
            <Button Content="Open Logs" Margin="8,0,0,0" Command="{Binding OpenLogsCommand}"/>
            <Button Content="Open Data" Margin="8,0,0,0" Command="{Binding OpenDataCommand}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Settings file:" Foreground="{StaticResource MutedBrush}"/>
            <TextBlock Text="{Binding SettingsFilePath}" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Paths (writability)" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBox Text="{Binding PathsStatus}" IsReadOnly="True" TextWrapping="Wrap"
                         Background="#0F1722" Foreground="{StaticResource TextBrush}" BorderBrush="#243143"
                         Height="200"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                <TextBlock Text="Subsystems" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding DbStatus}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding WebView2Status}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding AudioStatus}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="Log directory:" Foreground="{StaticResource MutedBrush}" Margin="0,10,0,0"/>
                <TextBlock Text="{Binding LogDirectory}" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Foreground="{StaticResource MutedBrush}"
                   Text="If something is broken, open logs and share the error. AuroraMusic should never fail silently."/>
    </Grid>
</UserControl>
