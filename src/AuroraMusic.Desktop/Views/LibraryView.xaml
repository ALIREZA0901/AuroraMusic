<UserControl x:Class="AuroraMusic.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Width="420" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Search" Margin="10,0,0,0" Command="{Binding SearchCommand}" />
                <Button Content="Rescan (My Music)" Margin="10,0,0,0" Command="{Binding RescanCommand}" />
            </StackPanel>
            <TextBlock Text="{Binding ScanStatus}" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Tracks}" SelectedItem="{Binding SelectedTrack}"
                  AutoGenerateColumns="False" Margin="0,14,0,14"
                  Background="#0F1722" Foreground="{StaticResource TextBrush}" BorderBrush="#243143"
                  HeadersVisibility="Column" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="*"/>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="*"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding DurationSeconds}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Play Selected" Command="{Binding PlaySelectedCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
